import React ,{Component} from 'react';
const appUtils ={
	var text = "";
	var possible = "012345678901234567890ABCDEFGHIJKLMNOPQRSTUVWXYZ09876543210123456789";
	for (var i = 0; i < lmt; i++)
	text += possible.charAt(Math.floor(Math.random() * possible.length));
}

const utils = appUtils;